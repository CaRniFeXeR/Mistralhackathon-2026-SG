mistralai==1.12.4
fastapi==0.133.1
python-multipart==0.0.22
uvicorn==0.30.6
uvloop>=0.19.0
httptools>=0.6.0
websockets==16.0
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20.0
greenlet>=3.0
pydantic>=2.0
python-jose[cryptography]==3.3.0
nanoid==2.0.0
httpx>=0.28.0
python-dotenv>=1.0.0

# vLLM (nightly) is NOT listed here — it is installed on-demand by run_app.sh
# when --ai-mode vllm is selected, because it requires a platform-specific
# nightly wheel from https://wheels.vllm.ai/nightly.
# Additional audio libs installed alongside vLLM: soxr librosa soundfile